[project]
name = "dag-imoveis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru",
    "pandas",
    "openpyxl",  # Para ler arquivos Excel
]
